window.getUserInput = () => {
    // Function body here
};

window.generateArmy = (age, location, ainu) => {
    // Function body here
};

window.calculateArmyWorth = (racesobj, armyObj) => {
    // Function body here
};

window.battleCalculator = () => {
    // Function body here
};

window.battleDuration = (eArmyWorth, gArmyWorth)=>{
    // Function body here
};